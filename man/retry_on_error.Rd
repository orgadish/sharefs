% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retry.R
\name{retry_on_error}
\alias{retry_on_error}
\title{Retry an action if it throws an error}
\usage{
retry_on_error(
  action,
  retries = 5,
  initial_wait_seconds = 0.05,
  jitter = TRUE,
  retryable = function(e) TRUE
)
}
\arguments{
\item{action}{A function/closure to execute.}

\item{retries}{Maximum number of total attempts. Values less than 1
are treated as 1 -- \code{action()} always runs at least once.}

\item{initial_wait_seconds}{Baseline backoff time.}

\item{jitter}{Logical. If \code{TRUE} (default), randomizes the sleep
intervals; see \code{\link[=get_backoff_wait]{get_backoff_wait()}}.}

\item{retryable}{A function taking the caught error condition and
returning \code{TRUE} if it's worth retrying, \code{FALSE} if it's certain to
fail again no matter how many times it's retried (e.g. a permission
error, as opposed to a transient network blip). Defaults to always
retryable. When \code{FALSE}, the error is re-raised immediately without
sleeping or consuming further attempts.}
}
\value{
The successful result of \code{action()}.
}
\description{
Calls \code{action()}, retrying with backoff if it throws. Returns the
successful result, or re-raises the final error (with its original
class and attributes intact) if every attempt fails.
}
