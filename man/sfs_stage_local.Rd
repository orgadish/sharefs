% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stage.R
\name{sfs_stage_local}
\alias{sfs_stage_local}
\title{Copy files to local disk before reading them}
\usage{
sfs_stage_local(files, dir = NULL)
}
\arguments{
\item{files}{A character vector of existing file paths to stage, or
the output of \code{\link[=sfs_dir_info]{sfs_dir_info()}}.}

\item{dir}{The local directory to copy files into. If \code{NULL} (default),
a new temporary directory is created.}
}
\value{
A tibble with columns \code{path}, \code{local_path}, \code{size}, and
\code{modification_time}. The staging directory is attached as the
\code{"sharefs_stage_dir"} attribute -- pass the result to
\code{\link[=sfs_stage_cleanup]{sfs_stage_cleanup()}} when done.
}
\description{
Copies \code{files} to a local directory using \code{robocopy} (multi-threaded,
grouped by source directory so each directory is one call). Reading
many small files locally is typically much faster than reading them
over a network share. Errors if \code{robocopy} is unavailable or fails --
no fallback, since \code{fs::file_copy()} doesn't preserve timestamps the
way \code{robocopy} does.

\code{files} can be a plain path vector or the output of \code{\link[=sfs_dir_info]{sfs_dir_info()}}
-- only \code{path} is used as the copy source. The result's
\code{size}/\code{modification_time} are re-derived from the staged copies
themselves, not carried through from any prior listing.
}
\examples{
src <- tempfile()
dir.create(src)
files <- file.path(src, c("a.txt", "b.txt"))
file.create(files)

staged <- sfs_stage_local(files)
staged

sfs_stage_cleanup(staged)
unlink(src, recursive = TRUE)
}
